apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Online Boutique (microservices-demo) pinned to release v0.10.5.
# Vendored manifest so deployments are reproducible and offline-capable.
resources:
  - kubernetes-manifests.yaml
